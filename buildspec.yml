version: 0.2

env:
  variables:
      CODEBUILDSOURCEBRANCH: ""
phases:
  install:
    commands:
      - echo $CODEBUILDSOURCEBRANCH Branch ! install phase...
  pre_build:
    commands:
      - echo $CODEBUILDSOURCEBRANCH Branch ! pre_build phase...
  build:
    commands:
      - echo $CODEBUILDSOURCEBRANCH Branch ! Build started on `date`
      - mvn install
  post_build:
    commands:
      - echo $CODEBUILDSOURCEBRANCH Branch ! Build completed on `date`
artifacts:
  files:
    - target/messageUtil-1.0.jar
  discard-paths: yes

